
// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-demo using AI Type Azure Open AI and AI Model gpt-4.1

ROOST_METHOD_HASH=getPayment_id_6e9ccc8b31
ROOST_METHOD_SIG_HASH=getPayment_id_6308770b57

[
  {
    "description": "getPayment_id() returns the most recently set value when setPayment_id() is called multiple times with different integers.",
    "category": "critical",
    "source": "llm",
    "priority": "high"
  },
  {
    "description": "getPayment_id() returns 0 after setPayment_id(0) is called on a Payment object.",
    "category": "critical",
    "source": "llm",
    "priority": "high"
  },
  {
    "description": "getPayment_id() does not alter the state of any other Payment fields when called.",
    "category": "recommended",
    "source": "llm",
    "priority": "medium"
  },
  {
    "description": "getPayment_id() returns correct value after setting payment_id via setPayment_id() followed by modifying other unrelated fields.",
    "category": "recommended",
    "source": "llm",
    "priority": "medium"
  }
]
*/

// ********RoostGPT********

package com.beko.DemoBank_v1.models;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Assertions;
import java.time.LocalDateTime;
import org.junit.jupiter.api.*;
import javax.persistence.Entity;
import javax.persistence.Id;

public class PaymentGetPaymentIdTest {

	// Scenario 1: getPayment_id() returns the most recently set value after multiple
	// setPayment_id() calls
	@Test
	public void getPaymentIdReturnsMostRecentValueAfterMultipleSets() {
		Payment payment = new Payment();
		payment.setPayment_id(5);
		payment.setPayment_id(42);
		payment.setPayment_id(-7);
		payment.setPayment_id(200);
		Assertions.assertEquals(200, payment.getPayment_id());
	}

	// Scenario 2: getPayment_id() returns 0 after setPayment_id(0) is called
	@Test
	public void getPaymentIdReturnsZeroAfterSetToZero() {
		Payment payment = new Payment();
		payment.setPayment_id(0);
		Assertions.assertEquals(0, payment.getPayment_id());
	}

	// Scenario 3: getPayment_id() does not alter any other Payment fields when called
	@Test
	public void getPaymentIdDoesNotAlterOtherFields() {
		Payment payment = new Payment();
		payment.setPayment_id(99);
		payment.setAccount_id(1);
		payment.setBeneficiary("Bob");
		payment.setBeneficiary_acc_no("BA123456");
		payment.setAmount(10.5);
		payment.setReference_no("REF123");
		payment.setStatus("pending");
		payment.setReason_code("R0");
		LocalDateTime dt = LocalDateTime.now();
		payment.setCreated_at(dt);
		// Save field values
		int accountId = payment.getAccount_id();
		String beneficiary = payment.getBeneficiary();
		String beneficiaryAccNo = payment.getBeneficiary_acc_no();
		double amount = payment.getAmount();
		String referenceNo = payment.getReference_no();
		String status = payment.getStatus();
		String reasonCode = payment.getReason_code();
		LocalDateTime createdAt = payment.getCreated_at();
		// Call getPayment_id
		int pid = payment.getPayment_id();
		// Assert other fields did not change
		Assertions.assertEquals(accountId, payment.getAccount_id());
		Assertions.assertEquals(beneficiary, payment.getBeneficiary());
		Assertions.assertEquals(beneficiaryAccNo, payment.getBeneficiary_acc_no());
		Assertions.assertEquals(amount, payment.getAmount());
		Assertions.assertEquals(referenceNo, payment.getReference_no());
		Assertions.assertEquals(status, payment.getStatus());
		Assertions.assertEquals(reasonCode, payment.getReason_code());
		Assertions.assertEquals(createdAt, payment.getCreated_at());
		// Assert payment_id is correct
		Assertions.assertEquals(99, pid);
	}

	// Scenario 4: getPayment_id() returns correct value after setPayment_id() and
	// changing other unrelated fields
	@Test
	public void getPaymentIdReturnsSetValueAfterModifyingOtherFields() {
		Payment payment = new Payment();
		payment.setPayment_id(23);
		payment.setAccount_id(100);
		payment.setBeneficiary("Alice");
		payment.setBeneficiary_acc_no("BA999999");
		payment.setAmount(94.25);
		payment.setReference_no("REF999");
		payment.setStatus("completed");
		payment.setReason_code("R1");
		payment.setCreated_at(LocalDateTime.now());
		Assertions.assertEquals(23, payment.getPayment_id());
	}

}