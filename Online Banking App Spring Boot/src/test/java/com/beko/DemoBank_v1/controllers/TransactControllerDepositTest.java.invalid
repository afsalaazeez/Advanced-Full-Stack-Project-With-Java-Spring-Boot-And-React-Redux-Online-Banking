//This test file is marked invalid as it contains compilation errors. Change the extension to of this file to .java, to manually edit its contents


// ********RoostGPT********
/*
Test generated by RoostGPT for test spring-advanced using AI Type Azure Open AI and AI Model gpt-4.1

ROOST_METHOD_HASH=deposit_d889dac99b
ROOST_METHOD_SIG_HASH=deposit_54d4c8262f

Scenario 1: Deposit with both deposit_amount and account_id empty

Details:  
  TestName: depositEmptyFieldsScenario
  Description: This test checks the behavior when both the deposit_amount and account_id fields are empty strings in the request map. It should validate that the controller returns a bad request with an appropriate error message.
Execution:
  Arrange: Create a request map with empty values for deposit_amount and account_id. Set up a mock HttpSession as required.
  Act: Invoke the deposit method passing the request map and session.
  Assert: Use JUnit to assert that ResponseEntity has HttpStatus.BAD_REQUEST and a message indicating fields cannot be empty.
Validation:  
  The assertion verifies that empty deposit_amount or account_id prevents deposits, ensuring basic input validation and security.


Scenario 2: Deposit with only deposit_amount empty

Details:  
  TestName: depositAmountEmptyScenario
  Description: This test checks the scenario where the deposit_amount is empty while account_id is present. It expects a bad request and a relevant message.
Execution:
  Arrange: Create a request map with deposit_amount as an empty string and a valid non-empty account_id. Set up mock HttpSession.
  Act: Call the deposit method with these parameters.
  Assert: Verify that the response is HttpStatus.BAD_REQUEST and contains the correct error message.
Validation:  
  The assertion proves that depositAmount being empty is independently checked and handled, supporting robust controller validation.


Scenario 3: Deposit with only account_id empty

Details:  
  TestName: accountIdEmptyScenario
  Description: This test validates the controller response when the account_id is empty but deposit_amount is present. It expects to receive a bad request and a descriptive message.
Execution:
  Arrange: Prepare request map with account_id as an empty string and deposit_amount as a valid value. Create mock HttpSession.
  Act: Invoke deposit using these values.
  Assert: Assert that ResponseEntity returns BAD_REQUEST status and the correct error explanation.
Validation:  
  This assertion provides coverage for missing account IDs, preventing business logic from executing with invalid accounts.


Scenario 4: Deposit amount is zero

Details:  
  TestName: depositAmountZeroScenario
  Description: This test examines how the deposit method reacts when deposit_amount provided is "0". It expects a bad request specifying that zero deposit amounts are not allowed.
Execution:
  Arrange: Setup request map with deposit_amount as "0" and a valid account_id. Mock HttpSession with user attribute as required.
  Act: Call deposit with these inputs.
  Assert: Assert response status as BAD_REQUEST with message about deposit amount not being zero.
Validation:  
  The assertion ensures that business rules reject zero-value deposits, thus avoiding invalid transaction attempts.


Scenario 5: Deposit with negative deposit_amount value

Details:  
  TestName: depositNegativeAmountScenario
  Description: This test covers the case when deposit_amount is a negative value string (e.g., "-100.0"). The method currently does not explicitly handle negative values, so the test would show whether such deposits are processed (for business review).
Execution:
  Arrange: Provide request map with deposit_amount as "-100.0" and a valid account_id. Mock session with valid user.
  Act: Invoke deposit with these parameters.
  Assert: Check the response for acceptance of negative deposits (e.g., successful response), and verify accountRepository method calls.
Validation:  
  This assertion highlights any missing business rule regarding negative deposits, indicating if such values should be rejected in future revisions.


Scenario 6: Deposit with valid positive deposit_amount

Details:  
  TestName: depositValidAmountScenario
  Description: This test validates the normal happy path, depositing a positive amount to a valid account, with proper user session. It expects a successful response with a confirmation message and updated account info.
Execution:
  Arrange: Create requestMap with deposit_amount as a valid positive string, and valid account_id. Mock session with valid user, mock repository methods to return expected values.
  Act: Call deposit.
  Assert: Assert response status is OK, message indicates success, and updated account list is present.
Validation:  
  Assertion confirms that deposits work with valid inputs, demonstrating the basic correctness of the core flow.


Scenario 7: Deposit with non-numeric deposit_amount string

Details:  
  TestName: depositAmountNonNumericScenario
  Description: This test checks what happens if deposit_amount contains non-numeric characters (e.g., "abc123"). Method throws NumberFormatException, so the application should handle it (but this may cause a crash if not caught).
Execution:
  Arrange: Provide a request map with deposit_amount as "abc123" and valid account_id. Mock session with valid user.
  Act: Call deposit with these values.
  Assert: Expect NumberFormatException or similar, depending on whether exception is caught or not.
Validation:  
  This assertion points out areas needing defensive programming; inputs should be validated for numeric format before processing.


Scenario 8: Deposit with non-numeric account_id string

Details:  
  TestName: accountIdNonNumericScenario
  Description: Scenario where account_id is a non-numeric string (e.g., "xyz"). Checks whether deposit properly rejects or fails (NumberFormatException expected).
Execution:
  Arrange: Set request map with deposit_amount as valid numeric, account_id as "xyz". Mock session with user.
  Act: Call deposit.
  Assert: Expect NumberFormatException due to invalid parsing.
Validation:  
  The assertion signals necessary type validation on account_id, preventing runtime errors and ensuring stability.


Scenario 9: Deposit with missing user session attribute

Details:  
  TestName: userSessionMissingScenario
  Description: Test scenario when the user attribute in session is missing (null). Method tries to cast and access user_id, which will throw NullPointerException.
Execution:
  Arrange: Mock HttpSession with no user attribute. Prepare normal request map.
  Act: Call deposit with these mocks.
  Assert: Expect NullPointerException, unless handled by external mechanism.
Validation:  
  This assertion demonstrates the importance of session management and user authentication checks before proceeding.


Scenario 10: Deposit with valid session but user object’s user_id is non-numeric

Details:  
  TestName: userIdNonNumericScenario
  Description: This test covers the case where user.getUser_id() returns a string that is not numeric, for example "abc". It expects NumberFormatException when parsing user_id.
Execution:
  Arrange: Mock user object with user_id="abc", set as session attribute. Prepare valid request map.
  Act: Call deposit.
  Assert: Expect NumberFormatException due to parsing error.
Validation:  
  Assertion highlights a needed business rule: user IDs should be validated for numeric integrity before use.


Scenario 11: Deposit with valid session and repositories throw exception

Details:  
  TestName: accountRepositoryExceptionScenario
  Description: Tests the controller's behavior when accountRepository or transactRepository methods throw unexpected exceptions during deposit.
Execution:
  Arrange: Mock user and session normally. Mock accountRepository.getAccountBalance or changeAccountsBalanceById to throw RuntimeException. Prepare valid request map.
  Act: Call deposit.
  Assert: Verify that an exception is thrown, or a generic error is returned if caught.
Validation:  
  This assertion validates controller robustness against repository or infrastructure errors.


Scenario 12: Deposit with valid user, account, and very large deposit_amount

Details:  
  TestName: depositLargeAmountScenario
  Description: This test checks depositing an unusually large amount. It should succeed if system handles large double values, otherwise show overflow/precision problems.
Execution:
  Arrange: requestMap with very large deposit_amount value (e.g., "1000000000.0"), valid account_id. Mock user and repositories.
  Act: Run deposit.
  Assert: Assert successful outcome or error if precision limits are exceeded.
Validation:  
  This scenario validates the method’s ability to process large-value deposits without risk of overflow.


Scenario 13: Deposit with minimal positive value (e.g., "0.01")

Details:  
  TestName: depositMinimalAmountScenario
  Description: Test depositing the smallest positive value allowed, to ensure that boundary conditions are handled and low-value deposits are accepted.
Execution:
  Arrange: Prepare requestMap with deposit_amount="0.01" and valid account_id. Mock user and session.
  Act: Call deposit.
  Assert: Assert successful deposit, message indicates success, response accounts are updated.
Validation:  
  This assertion proves method’s handling of minimal allowed values and acceptance of small legitimate deposits.


Scenario 14: Deposit with existing account and user but repositories return empty accounts

Details:  
  TestName: depositResponseEmptyAccountsScenario
  Description: Tests if deposit succeeds but accountRepository.getUserAccountsById returns empty list or map. It confirms deposit still returns a success message but no account data.
Execution:
  Arrange: Valid requestMap, user, and session. Mock getUserAccountsById to return empty collection.
  Act: Call deposit.
  Assert: Response message indicates success, accounts are empty.
Validation:  
  The assertion tests user feedback and completeness of response in edge cases where no accounts exist for user after deposit.


Scenario 15: Deposit with valid input and check repository method calls

Details:  
  TestName: depositRepositoryCallScenario
  Description: This test ensures the deposit method calls accountRepository.getAccountBalance, changeAccountsBalanceById, and transactRepository.logTransaction with correct arguments during a successful flow.
Execution:
  Arrange: Mock session, user, and repositories. Spy on repository method calls. Prepare valid requestMap.
  Act: Invoke deposit.
  Assert: Assert that the required repository methods were called with expected parameters.
Validation:  
  Assertion confirms internal transactional flow, critical for business logic, logging, and database consistency.


Scenario 16: Deposit with multiple concurrent requests

Details:  
  TestName: depositConcurrentRequestsScenario
  Description: Simulates concurrent deposit requests to the same account and user, checking for thread safety and data integrity.
Execution:
  Arrange: Prepare multiple requestMaps for the same account with valid deposit_amounts. Setup mocks for repositories.
  Act: Fire deposit calls concurrently.
  Assert: Validate that balances are correctly updated and no data races occur.
Validation:  
  Ensures that deposits are handled atomically, important for reliability in multi-user/banking environments.


Scenario 17: Deposit with old/current LocalDateTime value

Details:  
  TestName: depositDateTimeScenario
  Description: Checks if the date/time value is recorded accurately on transaction logs during deposit. Ensures that transaction date is the current time.
Execution:
  Arrange: Mock transactRepository, session, and repositories to capture currentDateTime. Prepare valid requestMap.
  Act: Run deposit.
  Assert: Assert that transactRepository.logTransaction is called with correct LocalDateTime argument.
Validation:  
  This assertion ensures auditability of deposits in banking, tracking proper transaction timing.


Scenario 18: Deposit where accountRepository.getAccountBalance returns negative balance

Details:  
  TestName: depositAccountNegativeBalanceScenario
  Description: Tests deposit logic for accounts that currently have negative balances, ensuring correct addition and reporting.
Execution:
  Arrange: Mock accountRepository.getAccountBalance to return negative value. Valid requestMap and session.
  Act: Call deposit.
  Assert: Assert new balance is correctly increased (becomes less negative or positive), and deposit is successful.
Validation:  
  Shows business handling of overdrawn accounts and that deposits properly update balances regardless of starting negative value.


Scenario 19: Deposit with deposit_amount containing leading/trailing spaces

Details:  
  TestName: depositAmountWithSpacesScenario
  Description: Tests whether deposit properly handles deposit_amount values with extra whitespace, expecting them to be trimmed or fail parsing.
Execution:
  Arrange: Set deposit_amount as " 500.0 ", valid account_id, user, and session.
  Act: Call deposit.
  Assert: Expect NumberFormatException or success after trimming, depending on implementation.
Validation:  
  This assertion highlights input hygiene, showing needed validation/cleanup logic for real-world user input handling.


Scenario 20: Deposit with account_id containing leading/trailing spaces

Details:  
  TestName: accountIdWithSpacesScenario
  Description: Validates deposit's behavior when account_id has additional whitespace. Checks whether parsing is robust or fails.
Execution:
  Arrange: Create requestMap with account_id as " 101 ", valid deposit_amount, user, and session.
  Act: Call deposit.
  Assert: Expect NumberFormatException or correct parsing as per method behavior.
Validation:  
  Advocates for input sanitization, making sure account selection works for typical human input scenarios.


Scenario 21: Deposit with decimal deposit_amount string

Details:  
  TestName: depositDecimalAmountScenario
  Description: Checks deposit with a decimal string deposit_amount (e.g., "123.45"), confirming that float/double processing is handled.
Execution:
  Arrange: Set requestMap with deposit_amount="123.45", valid account_id, user, session and repository mocks.
  Act: Call deposit.
  Assert: Success response, accountRepository methods receive correct double values.
Validation:  
  Ensures proper handling of floating point monetary values, verifying bank transaction logic for real-world decimals.


Scenario 22: Deposit with integer deposit_amount string

Details:  
  TestName: depositIntegerAmountScenario
  Description: Ensures deposit works with whole number string deposit_amount (e.g., "100"), updating account balances correctly.
Execution:
  Arrange: requestMap with deposit_amount="100", valid account_id, user, session, repositories.
  Act: Call deposit.
  Assert: Success, correct double value passed in repository methods.
Validation:  
  Validates handling of integer deposits, complementing scenario coverage for string-based numeric conversions.


Scenario 23: Deposit with very high account_id value

Details:  
  TestName: depositHighAccountIdScenario
  Description: Checks deposit behavior with extremely high account_id values (e.g., "999999"), ensuring correct parsing and repository usage.
Execution:
  Arrange: Set requestMap with valid deposit_amount, account_id="999999", user, session, repositories.
  Act: Call deposit.
  Assert: Success if system can handle such IDs, error otherwise.
Validation:  
  Completes boundary testing for account ID limits, critical for scalability and security.


Scenario 24: Deposit with accountRepository.getAccountBalance and accounts returning null

Details:  
  TestName: depositNullRepositoryReturnScenario
  Description: Tests the method when repository methods return null values. Checks if logic can cope or crashes.
Execution:
  Arrange: Mock accountRepository.getAccountBalance and getUserAccountsById to return null. Prepare valid requestMap and session.
  Act: Call deposit.
  Assert: Expect exception (e.g., NullPointerException) or graceful handling.
Validation:  
  Reveals error handling needs for unexpected repository responses, improving controller resilience.


Scenario 25: Deposit with malformed requestMap (missing keys)

Details:  
  TestName: depositMalformedRequestMapScenario
  Description: Verifies controller's reaction when requestMap lacks either deposit_amount or account_id keys entirely, simulating malformed client requests.
Execution:
  Arrange: Create requestMap with missing deposit_amount or account_id. Setup session and mocks.
  Act: Call deposit.
  Assert: Expect NullPointerException or error message, depending on code's handling.
Validation:  
  Highlights necessity for presence checks and robust parsing to avoid runtime errors due to incomplete requests.


*/

// ********RoostGPT********
package com.beko.DemoBank_v1.controllers;import com.beko.DemoBank_v1.models.User;
import com.beko.DemoBank_v1.repository.AccountRepository;
import com.beko.DemoBank_v1.repository.PaymentRepository;
import com.beko.DemoBank_v1.repository.TransactRepository;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.ArgumentCaptor;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.http.ResponseEntity;
import org.springframework.http.HttpStatus;
import javax.servlet.http.HttpSession;
import java.time.LocalDateTime;
import java.util.HashMap;
import java.util.Map;
import java.util.Collections;
import java.util.List;
import java.util.ArrayList;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;
import org.junit.jupiter.api.*;
import com.beko.DemoBank_v1.models.PaymentRequest;
import com.beko.DemoBank_v1.models.TransferRequest;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import javax.servlet.http.HttpServletRequest;

@ExtendWith(MockitoExtension.class)
class TransactControllerDepositTest {
    @Mock
    private AccountRepository accountRepository;
    @Mock
    private PaymentRepository paymentRepository;
    @Mock
    private TransactRepository transactRepository;
    @Mock
    private HttpSession session;
    @InjectMocks
    private TransactController transactController;
    private User mockUser;
    private static final String USER_ID = "123";
    private static final String ACCOUNT_ID = "101";
    private static final double INITIAL_BALANCE = 1000.0;
    private List<Object> dummyAccountList() {
        // Use type Object to match repository signature; in real this would be List<Account>
        return new ArrayList<>();
    }
    @BeforeEach
    void setUp() {
        mockUser = new User();
        mockUser.setUser_id(USER_ID);
    }
    @Test
    @Tag("invalid")
    public void testDepositEmptyFieldsScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "");
        requestMap.put("account_id", "");
        ResponseEntity result = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result.getStatusCode());
        assertEquals("Deposit amount and account ID cannot be empty.", (String) result.getBody());
    }
    @Test
    @Tag("invalid")
    public void testDepositAmountEmptyScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "");
        requestMap.put("account_id", ACCOUNT_ID);
        ResponseEntity result = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result.getStatusCode());
        assertEquals("Deposit amount and account ID cannot be empty.", (String) result.getBody());
    }
    @Test
    @Tag("invalid")
    public void testAccountIdEmptyScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", "");
        ResponseEntity result = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result.getStatusCode());
        assertEquals("Deposit amount and account ID cannot be empty.", (String) result.getBody());
    }
    @Test
    @Tag("invalid")
    public void testDepositAmountZeroScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        ResponseEntity result = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result.getStatusCode());
        assertEquals("Deposit amount cannot be zero.", (String) result.getBody());
    }
    @Test
    @Tag("boundary")
    public void testDepositNegativeAmountScenario() {
        // NOTE: Negative amounts will succeed due to business logic, enhancement needed to block negatives
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "-100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID)))
                .thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID)))
                .thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertNotNull(body.get("accounts"));
        // Suggestion: Business logic should prevent negative deposit amounts
    }
    @Test
    @Tag("valid")
    public void testDepositValidAmountScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "200.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID)))
                .thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID)))
                .thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertNotNull(body.get("accounts"));
    }
    @Test
    @Tag("invalid")
    public void testDepositAmountNonNumericScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "abc123");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        assertThrows(NumberFormatException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("invalid")
    public void testAccountIdNonNumericScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", "xyz");
        when(session.getAttribute("user")).thenReturn(mockUser);
        assertThrows(NumberFormatException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("invalid")
    public void testUserSessionMissingScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(null);
        assertThrows(NullPointerException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("invalid")
    public void testUserIdNonNumericScenario() {
        User invalidUser = new User();
        invalidUser.setUser_id("abc");
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(invalidUser);
        assertThrows(NumberFormatException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("integration")
    public void testAccountRepositoryExceptionScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(anyInt(), anyInt())).thenThrow(new RuntimeException("Repository failure"));
        assertThrows(RuntimeException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("boundary")
    public void testDepositLargeAmountScenario() {
        String largeAmount = "1000000000.0";
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", largeAmount);
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(0.0);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertNotNull(body.get("accounts"));
    }
    @Test
    @Tag("boundary")
    public void testDepositMinimalAmountScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "0.01");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertNotNull(body.get("accounts"));
    }
    @Test
    @Tag("integration")
    public void testDepositResponseEmptyAccountsScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(Collections.emptyList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertTrue(body.get("accounts") instanceof Iterable);
        assertEquals(0, ((Iterable) body.get("accounts")).spliterator().getExactSizeIfKnown());
    }
    @Test
    @Tag("integration")
    public void testDepositRepositoryCallScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "150.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        verify(accountRepository).getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID));
        verify(accountRepository).changeAccountsBalanceById(INITIAL_BALANCE + 150.0, Integer.parseInt(ACCOUNT_ID));
        verify(transactRepository).logTransaction(eq(Integer.parseInt(ACCOUNT_ID)), eq("deposit"), eq(150.0), eq("online"), eq("success"), eq("Deposit Transaction Successfull"), any(LocalDateTime.class));
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
    }
    @Test
    @Tag("integration")
    public void testDepositConcurrentRequestsScenario() throws Exception {
        Map<String, String> requestMap1 = new HashMap<>();
        requestMap1.put("deposit_amount", "100.0");
        requestMap1.put("account_id", ACCOUNT_ID);
        Map<String, String> requestMap2 = new HashMap<>();
        requestMap2.put("deposit_amount", "200.0");
        requestMap2.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        Runnable r1 = () -> transactController.deposit(requestMap1, session);
        Runnable r2 = () -> transactController.deposit(requestMap2, session);
        Thread t1 = new Thread(r1);
        Thread t2 = new Thread(r2);
        t1.start();
        t2.start();
        t1.join();
        t2.join();
        verify(accountRepository, atLeast(2)).getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID));
        verify(accountRepository, atLeast(2)).changeAccountsBalanceById(anyDouble(), eq(Integer.parseInt(ACCOUNT_ID)));
    }
    @Test
    @Tag("boundary")
    public void testDepositDateTimeScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ArgumentCaptor<LocalDateTime> captor = ArgumentCaptor.forClass(LocalDateTime.class);
        transactController.deposit(requestMap, session);
        verify(transactRepository).logTransaction(eq(Integer.parseInt(ACCOUNT_ID)), eq("deposit"), eq(100.0), eq("online"), eq("success"), eq("Deposit Transaction Successfull"), captor.capture());
        LocalDateTime dateTimeLogged = captor.getValue();
        assertNotNull(dateTimeLogged);
        assertTrue(dateTimeLogged.isBefore(LocalDateTime.now().plusSeconds(1)));
        assertTrue(dateTimeLogged.isAfter(LocalDateTime.now().minusMinutes(5))); // TODO adjust interval if needed
    }
    @Test
    @Tag("boundary")
    public void testDepositAccountNegativeBalanceScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "50.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(-100.0);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        Map<String, Object> body = (Map<String, Object>) response.getBody();
        assertEquals("Amount Deposited Successfully.", (String) body.get("message"));
        assertNotNull(body.get("accounts"));
        verify(accountRepository).changeAccountsBalanceById(-100.0 + 50.0, Integer.parseInt(ACCOUNT_ID));
    }
    @Test
    @Tag("boundary")
    public void testDepositAmountWithSpacesScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", " 500.0 ");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        assertThrows(NumberFormatException.class, () -> {
            // Will throw NumberFormatException due to spaces unless trim() is used in implementation
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("boundary")
    public void testAccountIdWithSpacesScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", " 101 ");
        when(session.getAttribute("user")).thenReturn(mockUser);
        assertThrows(NumberFormatException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("valid")
    public void testDepositDecimalAmountScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "123.45");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
        verify(accountRepository).changeAccountsBalanceById(INITIAL_BALANCE + 123.45, Integer.parseInt(ACCOUNT_ID));
    }
    @Test
    @Tag("valid")
    public void testDepositIntegerAmountScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
    }
    @Test
    @Tag("boundary")
    public void testDepositHighAccountIdScenario() {
        String highAccountId = "999999";
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", highAccountId);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(highAccountId))).thenReturn(INITIAL_BALANCE);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(dummyAccountList());
        ResponseEntity<?> response = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.OK, (HttpStatus) response.getStatusCode());
    }
    @Test
    @Tag("invalid")
    public void testDepositNullRepositoryReturnScenario() {
        Map<String, String> requestMap = new HashMap<>();
        requestMap.put("deposit_amount", "100.0");
        requestMap.put("account_id", ACCOUNT_ID);
        when(session.getAttribute("user")).thenReturn(mockUser);
        when(accountRepository.getAccountBalance(Integer.parseInt(USER_ID), Integer.parseInt(ACCOUNT_ID))).thenReturn((Double) null);
        when(accountRepository.getUserAccountsById(Integer.parseInt(USER_ID))).thenReturn(null);
        assertThrows(NullPointerException.class, () -> {
            transactController.deposit(requestMap, session);
        });
    }
    @Test
    @Tag("invalid")
    public void testDepositMalformedRequestMapScenario() {
        Map<String, String> requestMap = new HashMap<>();
        // Only one key
        requestMap.put("deposit_amount", "100.0");
        // account_id missing
        ResponseEntity result = transactController.deposit(requestMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result.getStatusCode());
        assertEquals("Deposit amount and account ID cannot be empty.", (String) result.getBody());
        // Now, deposit_amount missing
        Map<String, String> anotherMap = new HashMap<>();
        anotherMap.put("account_id", ACCOUNT_ID);
        ResponseEntity result2 = transactController.deposit(anotherMap, session);
        assertEquals(HttpStatus.BAD_REQUEST, (HttpStatus) result2.getStatusCode());
        assertEquals("Deposit amount and account ID cannot be empty.", (String) result2.getBody());
    }
}